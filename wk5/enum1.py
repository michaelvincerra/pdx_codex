words = ['complete', 'absolute', 'positive']

def enumerator(adverb: str) -> str:

    result = list()

    for index, word in enumerate(words):
        result.append('ly')

    print(result)

enumerator('word')

